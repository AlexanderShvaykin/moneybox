en:
  dry_schema:
    errors:
      int?: "must be a number"
      date?:
        failure: "invalid date format"
      filled?: "can't be empty"
      key?: "required parameter"
      included_in?: "must be one of the %{list}"
      excluded_from?: "must not be one of the: %{list}"
      size?:
        arg:
          default: "size must be %{num}"
          range: "size must be within %{left} - %{right}"

        value:
          string:
            arg:
              default: "length must be %{num}"
              range: "length must be within %{left} - %{right}"

